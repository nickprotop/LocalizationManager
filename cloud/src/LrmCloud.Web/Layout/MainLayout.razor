@inherits LayoutComponentBase
@inject AuthService AuthService
@inject NavigationManager Navigation
@inject IJSRuntime JS
@using LrmCloud.Web.Components

<MudThemeProvider @ref="_themeProvider" @bind-IsDarkMode="_isDarkMode" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1" Dense="@_isMobile">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@ToggleDrawer" />
        <MudLink Href="" Typo="Typo.h6" Color="Color.Inherit" Underline="Underline.None" Class="ml-3 d-none d-sm-flex">LRM Cloud</MudLink>
        <MudLink Href="" Typo="Typo.h6" Color="Color.Inherit" Underline="Underline.None" Class="ml-2 d-flex d-sm-none">LRM</MudLink>
        <MudDivider Vertical="true" FlexItem="true" Class="mx-2 d-none d-sm-flex" />
        <OrgSelector />
        <MudSpacer />
        <MudTooltip Text="Toggle dark mode">
            <MudToggleIconButton Toggled="@_isDarkMode"
                                 ToggledChanged="@OnDarkModeToggled"
                                 Icon="@Icons.Material.Filled.DarkMode"
                                 ToggledIcon="@Icons.Material.Filled.LightMode"
                                 Color="Color.Inherit"
                                 Size="@(_isMobile ? Size.Small : Size.Medium)" />
        </MudTooltip>
        <AuthorizeView>
            <Authorized>
                <MudMenu Icon="@Icons.Material.Filled.Person" Color="Color.Inherit" Size="@(_isMobile ? Size.Small : Size.Medium)">
                    <MudMenuItem Disabled="true">
                        <MudText Typo="Typo.body2" Color="Color.Secondary">@context.User.Identity?.Name</MudText>
                    </MudMenuItem>
                    <MudDivider />
                    <MudMenuItem Href="settings/profile" Icon="@Icons.Material.Filled.Person">Profile</MudMenuItem>
                    <MudMenuItem Href="settings/api-keys" Icon="@Icons.Material.Filled.Key">API Keys</MudMenuItem>
                    <MudMenuItem Href="settings/usage" Icon="@Icons.Material.Filled.Analytics">Usage</MudMenuItem>
                    <MudMenuItem Href="settings/billing" Icon="@Icons.Material.Filled.CreditCard">Billing</MudMenuItem>
                    <MudDivider />
                    <MudMenuItem OnClick="@Logout" Icon="@Icons.Material.Filled.Logout">Logout</MudMenuItem>
                </MudMenu>
            </Authorized>
            <NotAuthorized>
                <MudButton Variant="Variant.Text" Color="Color.Inherit" Href="login" Size="@(_isMobile ? Size.Small : Size.Medium)">Login</MudButton>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen"
               ClipMode="DrawerClipMode.Always"
               Elevation="2"
               Breakpoint="Breakpoint.Md"
               Variant="@(_isMobile ? DrawerVariant.Temporary : DrawerVariant.Mini)"
               OpenMiniOnHover="@(!_isMobile)">
        <NavMenu />
        <MudSpacer />
        <MudNavMenu>
            <MudNavLink Href="/" Icon="@Icons.Material.Filled.Home" Target="_self">Website</MudNavLink>
        </MudNavMenu>
    </MudDrawer>

    <MudMainContent>
        <OfflineBanner />
        <UpdateAvailableBanner />
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="@(_isMobile ? "pa-2" : "py-4")">
            <GlobalErrorBoundary>
                @Body
            </GlobalErrorBoundary>
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private MudThemeProvider? _themeProvider;
    private bool _drawerOpen = true;
    private bool _isDarkMode;
    private bool _isMobile;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Load saved theme preference (falls back to system preference if not set)
            var theme = await JS.InvokeAsync<string>("lrmTheme.getPreference");
            _isDarkMode = theme == "dark";
            await InvokeAsync(StateHasChanged);

            // Check if mobile on first render
            await CheckMobileBreakpoint();
        }
    }

    private async Task CheckMobileBreakpoint()
    {
        try
        {
            var width = await JS.InvokeAsync<int>("eval", "window.innerWidth");
            _isMobile = width < 960; // md breakpoint
            if (_isMobile)
            {
                _drawerOpen = false;
            }
            StateHasChanged();
        }
        catch
        {
            // Ignore JS interop errors during prerendering
        }
    }

    private void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

    private async Task OnDarkModeToggled(bool isDark)
    {
        _isDarkMode = isDark;
        await JS.InvokeVoidAsync("lrmTheme.setPreference", isDark ? "dark" : "light");
    }

    private async Task Logout()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("login", forceLoad: true);
    }
}
